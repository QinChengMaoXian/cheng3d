import { GetTypeCount } from '../core/Base';

const _g = GetTypeCount;

export const ComponentType = {
    Transform                   : _g(),
    Geometry                    : _g(),
    Material                    : _g(),
    Camera                      : _g(),
    Light                       : _g(),
    Skeleton                    : _g(),
};
